<main class="contenedor contenedor-formularios no-padding">
    <h1>
        <%= nombrePagina %>
    </h1>

    <form action="/user/admin/panel/new-group" class="default-form" method="post" enctype="multipart/form-data">
        <div class="campo">
            <label for="">Nombre</label>
            <input type="text" name="name" placeholder="Nuevo grupo">
        </div>
        <div class="campo descripcion">
            <label for="">Descripcion</label>
            <div class="contenedor-editor">
                <input type="hidden" name="description" id="x">
                <trix-editor input="x"></trix-editor>
            </div>
        </div>
        <div class="campo">
            <label for="">Categoria</label>
            <select name="categoryId">
                <option selected disabled>-- Elige una categoria --</option>
                <% categories.forEach(category=> { %>
                    <option value=<%= category.id %>>
                        <%= category.name %>
                    </option>
                <% }); %>
            </select>
        </div>
        <div class="campo">
            <label for="">Imagen</label>
            <input type="file" name="img">
        </div>
        <div class="campo">
            <label for="">Imagen</label>
            <input type="url" name="url" placeholder="Sitio web">
        </div>
        <div class="campo enviar">
            <input type="submit" value="Crear grupo" class="btn btn-rosa">
        </div>
    </form>
</main>